#include <stdio.h>
   // for single line comment
   /*
   for multiline comment
   */
int main() {

   /*int num = 24;
   float fnum = 6.6;
   char star = '*'; // to store characters use '' quotes only
   printf("the number is: %d \n", num); 
   printf("the star symbol is: %c \n",star);
   printf("the float value: %f \n",fnum);
   printf("");*/

   char name[] ="lakshay";
   int age=18;
   printf("Your name is: %s \n",name); //to print a string use %s
   printf("age is: %d \n",age);
   age=27;
   printf("new age is: %d \n",age);



   return 0;

}