#include <stdio.h>
int main() {

   int age=40;
   double gpa=6.6; //to represent decimal number
   char panda ='@'; // in single quotes only - to store single char only
   // to store a string :
   char string[]="my name is lakshay"; //this is also an array - Use double quotes only
   /*
   Array in C is immutable that means its value can't be changed once assigned..
   and also there is no string in C but we can use array in its place which is BAD..
   */
   return 0;

}